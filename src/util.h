#pragma once

#include <locale>
#include <iostream>
#include <string>
#include <sstream>
#include <vector>

static int appWindowSizeX = 1280;
static int appWindowSizeY = 800;

std::wstring to_wstring(const char* str);